# Copy this file to server/.env and fill in values

# Port for the Express server (default: 3001)
PORT=3001

# Bearer token for iOS Shortcut authentication.
# If set, requests to POST /api/steps that include a token must match this value.
# Leave empty to disable token auth (useful for local development).
API_TOKEN=your-secret-token-here
